# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# Local development
MONGODB_URI=mongodb://localhost:27017/mars_cargo

# Docker production (using credentials from docker-compose.yaml)
# MONGODB_URI=mongodb://marsadmin:marspassword123@mongodb:27017/mars_cargo?authSource=admin

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Update Intervals (in milliseconds)
# Resource refresh interval
RESOURCE_UPDATE_INTERVAL=5000

# Alert check interval
ALERT_CHECK_INTERVAL=10000
